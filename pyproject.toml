[project]
name = "pr-playground"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
testpaths = [".github/workflows/pr-processing/tests"]

[tool.ruff]
line-length = 100
